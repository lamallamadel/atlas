Phase 6 — benchmarks
bundle-stats.json ← npm run build:prod && npm run benchmark:bundle
bundle-baseline-pre-phase6.json ← copie manuelle optionnelle pour le % de réduction
phase6-deliverables.json ← statut livrable + pistes Lighthouse / axe
Lighthouse : npm start puis npm run lighthouse:ci
Axe Playwright : npm run benchmark:a11y
